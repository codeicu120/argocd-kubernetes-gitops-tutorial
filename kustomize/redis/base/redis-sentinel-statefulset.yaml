apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-sentinel
  namespace: redis
spec:
  replicas: 2
  selector:
    matchLabels:
      app: redis
      role: sentinel
  template:
    metadata:
      labels:
        app: redis
        role: sentinel
    spec:
      dnsPolicy: ClusterFirst
      containers:
        - name: sentinel
          image: redis:7
          command: ["redis-server", "/config/sentinel.conf", "--sentinel"]
          ports:
            - containerPort: 26379
          volumeMounts:
            - name: redis-conf
              mountPath: /config
      volumes:
        - name: redis-conf
          configMap:
            name: redis-config
            items:
              - key: sentinel.conf
                path: sentinel.conf