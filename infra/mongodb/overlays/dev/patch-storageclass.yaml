apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-rs
  namespace: mongo
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: local-path   # 👈 改成你集群里的